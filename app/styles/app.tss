".win": { backgroundColor: '#fff' }
".win[platform=ios]": { statusBarStyle: Ti.UI.iPhone.StatusBar.LIGHT_CONTENT }
".win[platform=android]": { exitOnClose: false, windowSoftInputMode: Ti.UI.Android.SOFT_INPUT_ADJUST_PAN | Ti.UI.Android.SOFT_INPUT_STATE_ALWAYS_HIDDEN }
// ".nav-visible[platform=ios]": { barColor: '#3c6ad2', navTintColor: '#fff', titleAttributes: { color: '#fff' }, translucent: false }
// ".nav-visible[platform=android]": {  }
// ".nav-hidden[platform=ios]": { navBarHidden: true }
// ".nav-hidden[platform=android]": { theme: 'Theme.AppCompat.NoTitleBar' }
// ".nav-shadow-hidden[platform=ios]": { barImage: '/images/blank.png', hideShadow: true }

	// "Button[platform=android]": { backgroundColor: 'transparent' }
	// "MenuItem[platform=android]": { showAsAction: Titanium.Android.SHOW_AS_ACTION_ALWAYS }
	// "TextField[platform=android]": { hintTextColor: '#c7c7cd' }

	// ".scroller": { contentWidth: '100%', layout: 'vertical', scrollType: 'vertical' }

	// ".ai": { /* height: Titanium.UI.SIZE, top: 20, bottom: 20, */ style: Titanium.UI.ActivityIndicatorStyle.DARK }

	// ".lv": { backgroundColor: 'transparent', separatorColor: '#d6d6d6', separatorInsets: { left: 0, right: 0 } }
	// // ".lv-noseparator": { separatorColor: 'transparent' }
	// // ".lv-noseparator[platform=ios]": { separatorStyle: Ti.UI.iPhone.ListViewSeparatorStyle.NONE }
		// // ".hv": { height: 8 }
		// // ".fv": { height: Titanium.UI.SIZE }
		// ".li": { backgroundColor: 'transparent', selectedBackgroundColor: '#d6d6d6' }
		// // ".li[platform=ios]": { selectionStyle: Ti.UI.iPhone.ListViewCellSelectionStyle.NONE }
			// // ".li-title": { left: 16, right: 16, color: '#000', font: { fontSize: 16 } }
			
	// ".txt-wrapper": {  }
	// ".txt-wrapper[platform=ios]": { clipMode: Ti.UI.iOS.CLIP_MODE_ENABLED }
		// ".txt": { color: '#000', font: { fontSize: 14 }, backgroundColor: 'transparent' }
		// ".txt-textfield": { height: 52, left: 10, right: 10 }
		// ".txt-textfield[platform=ios]": { paddingLeft: 0, paddingRight: 0, clearButtonMode: Ti.UI.INPUT_BUTTONMODE_ONFOCUS }
		// ".txt-textfield[platform=android]": { hintTextColor: '#777' }
		// ".txt-textarea": { height: '100%', left: 10, right: 10, suppressReturn: false }
		// ".txt-textarea[platform=ios]": {  }
		// ".txt-textarea[platform=android]": { hintTextColor: '#777' }
		// ".txt-textfield-next": { returnKeyType: Titanium.UI.RETURNKEY_NEXT }
		// ".txt-textfield-done": { returnKeyType: Titanium.UI.RETURNKEY_JOIN }
		// ".txt-textfield-email": { keyboardType: Titanium.UI.KEYBOARD_EMAIL }
		// ".txt-textfield-number": { keyboardType: Titanium.UI.KEYBOARD_NUMBERS_PUNCTUATION }
		// ".txt-textfield-password": { passwordMask: true }